# Local (CPU) tooling for the DFlash / EAGLE3 domain benchmark.
# The GPU benchmark itself runs on Modal (scripts/modal_run_vllm.py), which builds
# its own vLLM image — nothing GPU is installed here.

# DataGen/ — Claude-based prompt-dataset generator.
anthropic>=0.40
# WildDataGen/ — stream Hugging Face datasets (WildChat + dedicated sources).
datasets>=3.0.0
# scripts/*_charts.py — result charts.
matplotlib>=3.7

# GPU speculator benchmark (scripts/benchmark_vllm.py) is Modal-only; the Modal
# image installs vLLM (nightly-capable) + transformers itself:
#   pip install -U vllm --extra-index-url https://wheels.vllm.ai/nightly
