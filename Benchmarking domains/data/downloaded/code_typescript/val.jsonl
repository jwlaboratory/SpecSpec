{"prompt": "order an array of objectes typed CollegioVerbale {\nid: CollegioVerbaleID;\nidRicUdien: number;\ntipoMag: string;\ngradoMag: number;\nmagistrato?: AnagraficaMagistratoCollegio;\nriserva: boolean;\nrelatore: boolean;\noperatore: number;\ncomparso: boolean;\noggi: number;\n}\nput first all the objects with tipoMag === ‘PRE’ then all the objects with relatore true and order them by gradoMag ascendant  then all the objects with tipoMag===‘CON’ and order them by gradoMag ascendant and then at the bottom of array all the objects with riserva true and order them by gradoMag ascendant", "domain": "code_typescript"}
{"prompt": "分析代码private getRelatedFieldIds(datasheetId: string, foreignDatasheetData: any\n    , foreignDstIdToLookupFldIdsMap: { [datasheetId: string]: string[] }) {\n    const lookupLinkedFieldIds: string[] = foreignDstIdToLookupFldIdsMap[datasheetId] || [];\n    const relatedFieldIds = new Set<string>([...lookupLinkedFieldIds]);\n    // Get view and field data of linked datasheet\n    const { views } = foreignDatasheetData.snapshot.meta;\n    // Primary field ID\n    const { fieldId } = head((head(views) as IViewProperty).columns)!;\n    relatedFieldIds.add(fieldId);\n    return [...relatedFieldIds];\n  }", "domain": "code_typescript"}
{"prompt": "refactor  c++  code  below ,QtConcurrent::run([=]() {\n        auto lic = SntlLicGen::getInstance(1);\n        qDebug()<<\"Update license expiration in background\";\n        QString rstrdStr, msgStr;\n        const auto featId = lic->getFeature();\n        if(featId < 0){\n            msgStr = QObject::tr(\"Hasp internal user type error.\");\n        }\n        bool succ = lic->getRemainTime(rstrdStr, featId);\n        bool showMsg = false;\n        if(!succ){\n            msgStr = lic->getHaspError();\n        }else{\n            if(rstrdStr.compare(\"perpetual\", Qt::CaseInsensitive) == 0){\n                showMsg = false;\n            }else{\n                bool isok = false;\n                double totalTime = QVariant(rstrdStr).toDouble(&isok);\n                if(isok){\n                    if(totalTime < 0){\n                        quint16 minute = 60;\n                        quint16 hour = minute * 60;\n                        quint64 day = hour * 24;\n                        quint64 curDay = fabs(totalTime) / day;\n                        quint64 curHour = (fabs(totalTime) - curDay * day) / hour;\n                        quint64 curMinute = (fabs(totalTime) - curDay * day - curHour * hour) / minute;\n                        if(curDay < 7 || (curDay == 7 && curHour == 0 && curMinute == 0)){\n                            msgStr = QObject::tr(\"Your product license will expire in \")\n                                     + QString::number(curDay) + \" \" + QObject::tr(\"day\") + \" \"\n                                     + QString::number(curHour) + \" \"+ QObject::tr(\"hour\")+ \" \"\n                                     + QString::number(curMinute) + \" \"+ QObject::tr(\"minute\");\n                            if(curDay <= 0){\n                                msgStr = QObject::tr(\"Your product license will expire in \") + \" \"+\n                                         QString::number(curHour) + \" \"+ QObject::tr(\"hour\") + \" \"+\n                                         QString::number(curMinute) + \" \"+ QObject::tr(\"minute\");\n                            }\n                            showMsg = true;\n                        }\n                    }else{\n                        msgStr = QObject::tr(\"The feature is expired.\");\n                        showMsg = true;\n                    }\n                }else{\n                    msgStr = QObject::tr(\"Internal error occurred when parsing time from memory.\");\n                    showMsg = true;\n                }\n            }\n        }\n        qDebug()<<\"License message:\"<<msgStr;\n        if(showMsg){\n            Q_EMIT toShowMessage(QObject::tr(\"Andromeda License Message\"), msgStr);\n        }\n    });", "domain": "code_typescript"}
{"prompt": "bool EncodeJsonStopResp(\n  const std::shared_ptr<StopRollPollingParameter>& stop_roll_polling_param,\n  bool success,\n  std::string* resp_body) {\n  base::EasyJson ej;\n  ej.SetObject();\n  ej.Set(\"RequestId\", stop_roll_polling_param->request_id);\n\n  resp_body->assign(ej.ToString());\n  return true;这段代码什么意思", "domain": "code_typescript"}
{"prompt": "import {\ninit,\ndispose,\nChart,\nDeepPartial,\nIndicatorFigureStylesCallbackData,\nIndicator,\nIndicatorStyle,\nKLineData,\nutils,\n} from “klinecharts”;\n\nconst chart = useRef<Chart|null>();\nconst ref = useRef<HTMLDivElement>(null);\n\nuseEffect(() => {\nchart.current = init(chart-${tradeId}, {styles: chartStyles});\n\nreturn () => dispose(chart-${tradeId});\n}, [tradeId]);\nuseEffect(() => {\nchart.current?.applyNewData(candles);\nchart.current?.overrideIndicator({\nname: “VOL”,\nshortName: “Объем”,\ncalcParams: [],\nfigures: [\n{\nkey: “volume”,\ntitle: “”,\ntype: “bar”,\nbaseValue: 0,\nstyles: (data: IndicatorFigureStylesCallbackData<Vol>, indicator: Indicator, defaultStyles: IndicatorStyle) => {\nconst kLineData = data.current.kLineData as KLineData;\nlet color: string;\n\nif (kLineData.close > kLineData.open) {\ncolor = utils.formatValue(indicator.styles, “bars[0].upColor”, (defaultStyles.bars)[0].upColor) as string;\n} else if (kLineData.close < kLineData.open) {\ncolor = utils.formatValue(indicator.styles, “bars[0].downColor”, (defaultStyles.bars)[0].downColor) as string;\n} else {\ncolor = utils.formatValue(indicator.styles, “bars[0].noChangeColor”, (defaultStyles.bars)[0].noChangeColor) as string;\n}\n\nreturn {color};\n},\n},\n],\n}, paneId.current);\nchart.current?.createIndicator(“VOL”, false, {id: paneId.current});\nchart.current?.setPriceVolumePrecision(+pricePrecision, +quantityPrecision);\n}, [candles]);\n\n<Box\nref={ref}\nid={chart-${tradeId}}\nwidth=“calc(100% - 55px)”\nheight={!handle.active ? 550 : “100%”}\nsx={{borderLeft: “2px solid #F5F5F5”}}\n/ >\n\nбиблиотека klinecharts, график размытый какой-то, как можно добавить четкости графику?", "domain": "code_typescript"}
{"prompt": "@Mapper\npublic interface ActivityDao {\n\n    @Insert(\"\")\n    int save(@Param(\"content\") String content,\n             @Param(\"hash\") String hash,\n             @Param(\"tradeDate\") String tradeDate,\n             @Param(\"tradeTime\") String tradeTime,\n             @Param(\"tableName\") String tableName);\n\n}\n\n\n在@Insert(\"\")中写一个批量插入", "domain": "code_typescript"}
{"prompt": "почему эта процедура не сохраняет файл как текстовый несмотря на то что у меня стоит фильтр\nvar f:textFile;\n    name:string;\n    i,j:integer;\nbegin\n  for i := 0 to n-1 do\n    for j := 0 to n-1 do\n      if StringGrid1.Cells[i,j]<>'' then\n        matr[i,j]:=StrToFloat(StringGrid1.Cells[j,i])\n      else\n        matr[i,j]:=0;\n  if savedialog.Execute then\n    begin\n      name:=savedialog.FileName;\n      assignfile(f,name);\n      rewrite(f);\n      Write_matr(f,matr,n);\n      closefile(f);\n    end;\nend;", "domain": "code_typescript"}
