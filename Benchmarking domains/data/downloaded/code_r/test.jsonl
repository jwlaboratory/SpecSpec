{"prompt": "results <- do.call(rbind, lapply(1:nrow(combinations), function(i) {\n    cur_combo <- combinations[i, ]\n    variable <- cur_comboVariable[[1]]\n    data_diff <- ifelse(cur_combo$Type == \"level\", variable, diff(variable))\n    result <- unit_root_tests(data_diff, \"level\", lags, cur_combo$Trend)\n    resultVarName <- cur_combo\n    VarName\n    resultType <- cur_combo$Type\n    resultTrend <- cur_combo$Trend\n    return(result)\n  }))\n\nError showing - \n\nError: object 'combinations' not found\n\nHow to solve it?", "domain": "code_r"}
{"prompt": "вот матрица знакомств на языке R.\nlibrary(igraph)\n\n\n\nmatrica<-matrix(rep(0,400), nrow=20,ncol=20)\nfor (i in 1:20) {\nfor (j in 1:20) {\nif(i==j || i%%5==0 || j%%5==0){\nmatrica[i,j]=1\n}\n}\n}\n# создаем объект графа из матрицы смежности\ng <- graph_from_adjacency_matrix(matrica, mode=“undirected”)\n\n\n# проверяем, что граф является связным\nis.connected(g) # TRUE\nнужно разбить на 2 графа,каждый из которых обязательно будет связным", "domain": "code_r"}
